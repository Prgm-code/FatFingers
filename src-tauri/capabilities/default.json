{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for FatFingers desktop windows",
  "windows": ["helper", "settings", "onboarding"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
